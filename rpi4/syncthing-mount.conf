[Unit]
# Obsidian's synced folder lives on the HDD (see install-service.sh); don't
# start syncthing until it's actually mounted at /mnt/Axagon, so a missing
# drive fails loudly instead of silently syncing into an empty directory.
RequiresMountsFor=/mnt/Axagon
